# ML Placeholder

This directory is intentionally reserved for the future whale-scoring and market-prediction model code.

Planned contents:
- feature builders
- experiment notebooks/scripts
- LightGBM/XGBoost model training code
- model registry metadata

The data platform is being built first so the eventual model choice can plug into a stable dataset.
